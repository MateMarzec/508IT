@{ ViewData["Title"] = "AddUserToRole"; }
<h3 class="text-center lightfont">Add User To Role</h3>
<hr />
<div class="container">
    <div class="row">
        <div class="col-6 offset-3">
            <form asp-action="AddUserToRole" method="post">
                <div class="form-group lightfont">
                    <label>Select UserName</label>
                    <select name="UserId" asp-items="@ViewBag.Users" class="form-control">
                        <option selected disabled>--Select User--</option>
                    </select>
                </div>
                <div class="form-group lightfont">
                    <label>Select Role Name</label>
                    <select name="RoleName" asp-items="@ViewBag.Roles" class="form-control">
                        <option selected disabled>--Select Role</option>
                    </select>
                </div>
                <input type="submit" class="btn btn-primary btn-block" value="Assign" />
                <a asp-action="Index" class="btn btn-danger btn-block">Cancel</a>
            </form>
        </div>
    </div>
</div>


